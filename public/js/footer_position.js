window.addEventListener("load", onLoad);
window.addEventListener("resize", onLoad);

function onLoad() {

    /*let filhos = document.querySelectorAll("body > div");
    let nav = document.querySelector("body > nav");
    let footer = document.querySelector("body > footer");
    let soma = 0;
    for (let i = 0; i < filhos.length ; i++) {
        soma += filhos[i].clientHeight;
    }
    let adicionar = window.innerHeight - nav.offsetHeight - (76) - soma +filhos[filhos.length - 1].clientHeight;
    if (adicionar < window.innerHeight && filhos.length > 0) {
        console.log(soma);
        filhos[filhos.length - 1].style.height = adicionar + "px";
    }*/
}